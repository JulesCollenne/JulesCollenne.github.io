export default function BackgroundNet({ density, maxSpeed, connectDist, dotSize, dark, }: {
    density?: number;
    maxSpeed?: number;
    connectDist?: number;
    dotSize?: number;
    dark?: boolean;
}): import("react/jsx-runtime").JSX.Element;
